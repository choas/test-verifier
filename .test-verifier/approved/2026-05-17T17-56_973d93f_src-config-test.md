---
id: tv_2026-05-17T17-56_973d93f_src-config-test
created_at: 2026-05-17T17:56:49Z
severity: CRITICAL
status: approved
llm_enriched: true
llm_model: gemma4:31b-cloud
test_file: "src/config.test.ts"
test_functions:
  - "returns defaults when called with no arguments"
  - "returns defaults when called with empty object"
  - "overrides specific fields while keeping other defaults"
  - "overrides llm provider"
  - "overrides policy settings"
  - "overrides matcher transitions"
  - "overrides snapshot config"
  - "overrides audit config"
  - "rejects invalid severity value"
  - "rejects invalid llm provider"
  - "rejects negative cacheTtlDays"
  - "rejects invalid truncation strategy"
  - "parses empty object to full defaults"
  - "default matcher transitions have 11 entries"
prod_files_related:
  - "src/commands/check.ts"
  - "src/commands/init.ts"
  - "src/config.ts"
  - "src/git.ts"
commit: 973d93f0bd5ff9da97d0f8278bd32d13849771e5
parent_commit: 25b4a8557f49f3242e656eb4e4bdc8fed51d3027
diff_hash: sha256:e51a9827ea1b1d6c3d6ce64d290c308c547ae058fc4b06bf64940ad4858546fa
---

# Test change in `src/config.test.ts`

## Findings (rule engine)

- **CRITICAL** Assertion "toEqual" removed from test "defineConfig > returns defaults when called with no arguments" without replacement
- **CRITICAL** Assertion "toEqual" removed from test "defineConfig > returns defaults when called with empty object" without replacement
- **SUSPICIOUS** Expected value changed from ["**/*.test.ts", "**/*.spec.ts", "**/*.test.svelte.ts"] to [
      "**/*.test.ts",
      "**/*.spec.ts",
      "**/*.test.js",
      "**/*.spec.js",
      "**/*.test.tsx",
      "**/*.spec.tsx",
      "**/*.test.jsx",
      "**/*.spec.jsx",
      "**/*.test.mts",
      "**/*.spec.mts",
      "**/*.test.mjs",
      "**/*.spec.mjs",
      "**/*.test.svelte.ts",
    ] in toEqual(). The test subject config.testGlobs is unchanged — verify this isn't chasing a buggy implementation.
- **SUSPICIOUS** Expected value changed from ["**/*.test.ts", "**/*.spec.ts", "**/*.test.svelte.ts"] to [
      "**/*.test.ts",
      "**/*.spec.ts",
      "**/*.test.js",
      "**/*.spec.js",
      "**/*.test.tsx",
      "**/*.spec.tsx",
      "**/*.test.jsx",
      "**/*.spec.jsx",
      "**/*.test.mts",
      "**/*.spec.mts",
      "**/*.test.mjs",
      "**/*.spec.mjs",
      "**/*.test.svelte.ts",
    ] in toEqual(). The test subject config.testGlobs is unchanged — verify this isn't chasing a buggy implementation.

## Diff

```diff
diff --git a/src/config.test.ts b/src/config.test.ts
index 99712c7..a127bcb 100644
--- a/src/config.test.ts
+++ b/src/config.test.ts
@@ -5,7 +5,21 @@ import { Severity } from "./types";
 describe("defineConfig", () => {
   test("returns defaults when called with no arguments", () => {
     const config = defineConfig();
-    expect(config.testGlobs).toEqual(["**/*.test.ts", "**/*.spec.ts", "**/*.test.svelte.ts"]);
+    expect(config.testGlobs).toEqual([
+      "**/*.test.ts",
+      "**/*.spec.ts",
+      "**/*.test.js",
+      "**/*.spec.js",
+      "**/*.test.tsx",
+      "**/*.spec.tsx",
+      "**/*.test.jsx",
+      "**/*.spec.jsx",
+      "**/*.test.mts",
+      "**/*.spec.mts",
+      "**/*.test.mjs",
+      "**/*.spec.mjs",
+      "**/*.test.svelte.ts",
+    ]);
     expect(config.excludeGlobs).toEqual(["**/node_modules/**"]);
     expect(config.llm.provider).toBe("anthropic");
     expect(config.llm.model).toBe("claude-sonnet-4-7");
@@ -19,7 +33,21 @@ describe("defineConfig", () => {
 
   test("returns defaults when called with empty object", () => {
     const config = defineConfig({});
-    expect(config.testGlobs).toEqual(["**/*.test.ts", "**/*.spec.ts", "**/*.test.svelte.ts"]);
+    expect(config.testGlobs).toEqual([
+      "**/*.test.ts",
+      "**/*.spec.ts",
+      "**/*.test.js",
+      "**/*.spec.js",
+      "**/*.test.tsx",
+      "**/*.spec.tsx",
+      "**/*.test.jsx",
+      "**/*.spec.jsx",
+      "**/*.test.mts",
+      "**/*.spec.mts",
+      "**/*.test.mjs",
+      "**/*.spec.mjs",
+      "**/*.test.svelte.ts",
+    ]);
   });
 
   test("overrides specific fields while keeping other defaults", () => {
```

## Analysis

**Summary:** Updated test expectations for default test globs to match an expanded list of supported file extensions in the production config.

**Risk Assessment:** SAFE

**Recommendation:** The change is a legitimate update to test expectations following a production change in `src/config.ts` that expanded the default `testGlobs` list. The rule engine's 'removed assertion' warnings are false positives caused by the multi-line array formatting.

## Decision

approved by 138015+choas@users.noreply.github.com
rationale: The rule engine's 'removed assertion' warnings are false positives caused by the multi-line array formatting.
signature: ed25519:3QwqS2gSu5bBh3ilBnjFoORwCY9SLhJyfAc3aPEV2+BvWokO5lVzOKDt9aNeID62QTAOYsuExuoY/D3KWRVtDA==
