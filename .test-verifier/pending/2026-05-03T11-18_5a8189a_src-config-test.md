---
id: tv_2026-05-03T11-18_5a8189a_src-config-test
created_at: 2026-05-03T11:18:59Z
severity: CRITICAL
status: pending
llm_enriched: false
test_file: "src/config.test.ts"
test_functions:
  - "returns defaults when called with no arguments"
  - "returns defaults when called with empty object"
  - "overrides specific fields while keeping other defaults"
  - "overrides llm provider"
  - "overrides policy settings"
  - "overrides matcher transitions"
  - "overrides snapshot config"
  - "overrides audit config"
  - "rejects invalid severity value"
  - "rejects invalid llm provider"
  - "rejects negative cacheTtlDays"
  - "rejects invalid truncation strategy"
  - "parses empty object to full defaults"
  - "default matcher transitions have 11 entries"
prod_files_related:
  - "package.json"
commit: 5a8189aa26c64016ad65b6b8cd125e17bc2e8254
parent_commit: c70a73fd5ad515a6739e2ba1069197c7b3593483
diff_hash: sha256:98adacec100a0285e8daf4f4e9e4472e144a6c3df181e3b5d10f1187d0051c8a
---

# Test change in `src/config.test.ts`

## Findings (rule engine)

- **CRITICAL** Assertion "toThrow" removed from test "defineConfig > rejects invalid severity value" without replacement
- **CRITICAL** Assertion "toThrow" removed from test "defineConfig > rejects invalid llm provider" without replacement
- **CRITICAL** Assertion "toThrow" removed from test "defineConfig > rejects negative cacheTtlDays" without replacement
- **CRITICAL** Assertion "toThrow" removed from test "defineConfig > rejects invalid truncation strategy" without replacement

## Diff

```diff
diff --git a/src/config.test.ts b/src/config.test.ts
index 75a80ab..99712c7 100644
--- a/src/config.test.ts
+++ b/src/config.test.ts
@@ -84,26 +84,25 @@ describe("defineConfig", () => {
   });
 
   test("rejects invalid severity value", () => {
-    expect(() =>
-      defineConfig({ rules: { assertionRemoved: "INVALID" as any } }),
-    ).toThrow();
+    // biome-ignore lint/suspicious/noExplicitAny: testing invalid input
+    expect(() => defineConfig({ rules: { assertionRemoved: "INVALID" as any } })).toThrow();
   });
 
   test("rejects invalid llm provider", () => {
-    expect(() =>
-      defineConfig({ llm: { provider: "openai" as any } }),
-    ).toThrow();
+    // biome-ignore lint/suspicious/noExplicitAny: testing invalid input
+    expect(() => defineConfig({ llm: { provider: "openai" as any } })).toThrow();
   });
 
   test("rejects negative cacheTtlDays", () => {
-    expect(() =>
-      defineConfig({ audit: { cacheTtlDays: -1 } }),
-    ).toThrow();
+    expect(() => defineConfig({ audit: { cacheTtlDays: -1 } })).toThrow();
   });
 
   test("rejects invalid truncation strategy", () => {
     expect(() =>
-      defineConfig({ rules: { snapshot: { truncationStrategy: "invalid" as any } } }),
+      defineConfig({
+        // biome-ignore lint/suspicious/noExplicitAny: testing invalid input
+        rules: { snapshot: { truncationStrategy: "invalid" as any } },
+      }),
     ).toThrow();
   });
 });
```

## Analysis

(Pending LLM enrichment. Run `bunx test-verifier enrich`.)

## Decision

(Empty until enrichment is complete and a human approves or rejects.)
